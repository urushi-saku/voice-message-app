# ========================================
# 環境変数サンプルファイル
# ========================================
# このファイルをコピーして .env として保存し、適切な値を設定してください

# MongoDB接続URL
# ローカル開発の場合
MONGODB_URI=mongodb://localhost:27017/voice_message_app

# MongoDB Atlas（クラウド）の場合は以下のような形式
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/voice_message_app

# JWT秘密鍵（ランダムな文字列を設定）
JWT_SECRET=your_jwt_secret_key_here_change_this_in_production

# サーバーポート番号
PORT=3000

# Node環境（development / production）
NODE_ENV=development
# ========================================
# Firebase Cloud Messaging設定
# ========================================
# Firebase Console からダウンロードした秘密鍵ファイルのパス
# 指定しない場合は ./config/serviceAccountKey.json を使用
# FIREBASE_SERVICE_ACCOUNT_KEY=./config/serviceAccountKey.json

# ========================================
# CORS設定
# ========================================
# フロントエンドのURLを指定（ローカル開発時）
# CORS_ORIGIN=http://localhost:8080

# ========================================
# ファイルアップロード設定
# ========================================
# アップロードディレクトリ
# UPLOAD_DIR=./uploads
# ========================================
# SMTP設定（パスワードリセットメール送信用）
# ========================================
# 未設定の場合はリセットURLをサーバーコンソールに出力（開発時確認用）

# --- Gmail を使う場合 ---
# 事前に「アプリパスワード」を発行してください
# Googleアカウント → セキュリティ → 2段階認証 → アプリパスワード
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_16char_app_password

# --- Outlook / Hotmail を使う場合 ---
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_email@outlook.com
# SMTP_PASS=your_password

# --- SendGrid を使う場合（本番推奨）---
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=your_sendgrid_api_key

# パスワードリセットリンクのベースURL
# モバイルアプリの場合はディープリンクスキームに変更してください
# 例: voicemsg://reset-password
FRONTEND_URL=http://localhost:3000

# ========================================
# Sentry エラーモニタリング
# ========================================
# Sentry プロジェクトの DSN（Data Source Name）
# https://sentry.io でプロジェクト作成後に取得
# 未設定の場合は Sentry は自動的に無効化されます
# SENTRY_DSN=https://xxxxxxxxxxxxxxxx@oXXXXXX.ingest.sentry.io/XXXXXXX

# ========================================
# Redis キャッシュ設定
# ========================================
# Redis サーバーのホストとポート
# 未設定の場合は Redis は自動的に無効化されます（アプリは正常動作）
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_PASSWORD=
