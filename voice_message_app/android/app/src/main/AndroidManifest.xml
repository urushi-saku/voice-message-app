<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ========================================
         Android パーミッション（アプリが使う機能の許可設定）
         ========================================
         以下のパーミッションはアプリが必要とする機能をAndroidに伝えるものです。
         Androidユーザーはアプリをインストール時または使用時にこれらの許可を与える必要があります。
    -->
    
    <!-- RECORD_AUDIO: マイクの使用許可
         説明: アプリがスマートフォンのマイクから音声を録音するために必要
         用途: 音声メッセージの録音機能で使用 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <!-- WRITE_EXTERNAL_STORAGE: 外部ストレージへの書き込み許可
         説明: アプリがスマートフォンの外部ストレージ（SDカードやダウンロードフォルダなど）
               にファイルを保存するために必要
         用途: 録音した音声ファイルをスマートフォンに保存するために使用 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    
    <!-- READ_EXTERNAL_STORAGE: 外部ストレージの読み込み許可
         説明: アプリが外部ストレージに保存されたファイルを読み込むために必要
         用途: 以前に保存した音声ファイルを再生するときに使用 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    
    <!-- INTERNET: インターネット接続の許可
         説明: アプリがインターネットを使用してサーバーと通信するために必要
         用途: 音声ファイルをサーバーにアップロードしたり、サーバーから音声ファイルを
               ダウンロードしたりするときに使用 -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <application
        android:label="voice_message_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
